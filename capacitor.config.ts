import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.test.driver',
  appName: 'AmbiPalmDriver',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
